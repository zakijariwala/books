# This repository holds book-writing skeletons, not a book. Each skeleton carries
# its own .gitignore for build artefacts; this one only keeps stray OS and editor
# files out of the top level.
.DS_Store
*.swp
__pycache__/
.pytest_cache/
