# Compute: virtual machines and serverless

<!-- DRAFT SKELETON. Budget 1,600 words.
     Blocked on: Cloud Run Fundamentals module list, Elastic GCP Infrastructure
     module list. Machine families and service limits move fast. Verify every
     number against current docs before drafting prose. -->

Opening scenario: a fleet of VMware guests and a handful of bare-metal boxes
running things nobody wants to touch. 150 words.

*[FIGURE 4.1: on-prem hypervisor estate beside GCE machine families, Cloud Run,
and Cloud Functions, showing which workloads land where]*

## Compute Engine

- Machine families and when each wins: general purpose, compute optimised,
  memory optimised, accelerator
- Sole-tenant nodes, and the licensing requirement that drives them
- Live migration, which has no AWS equivalent and changes maintenance planning
- Spot VMs against committed use discounts. Chapter 12 carries the cost detail

## Managed instance groups

- Autohealing, regional distribution, and why regional beats zonal by default
- Rolling updates as the rollback story for large fleets
- Instance templates as the immutable unit

## Serverless

- Cloud Run as the default for containerised request-response workloads
- Cloud Run jobs against services
- Cloud Functions and where it still fits
- App Engine, and why it rarely wins new work now
- Cold start, concurrency, and the request timeout ceiling

*[FIGURE 4.2: decision flowchart selecting VM, container, or function from
workload shape]*

## Exam traps

- Licensing that requires physical core visibility points to sole-tenant nodes
- Long-running or stateful work rules out request-scoped serverless
- "Minimise operational overhead" plus containers usually means Cloud Run
  rather than GKE

## Recap

Five bullets.
